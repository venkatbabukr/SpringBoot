spring:
  application.name: springcloud-demo-service1
server:
  port: 8081
management:
  info:
    java.enabled: true
    env.enabled: true
  endpoints.web:
    exposure.include: health,info
eureka:
  instance:
    hostname: localhost